import React from "react";
import ReactDOM from "react-dom/client";
import MorseTrad from "./morseTrad.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MorseTrad />
  </React.StrictMode>
);
